<Page x:Class="WpfApp.pages.pgGallery"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="pgGallery">
    <Page.Resources>
        <DataTemplate x:Key="dtOutputImages">
            <StackPanel>
                <Image x:Name="UserImage" Loaded="UserImage_Loaded" Uid="{Binding id}" Width="300" Height="300"/>
            </StackPanel>

        </DataTemplate>
    </Page.Resources>

    <Grid>
        <ListBox Name="lbUsers" ItemTemplate="{StaticResource dtOutputImages}"/>
    </Grid>
</Page>
